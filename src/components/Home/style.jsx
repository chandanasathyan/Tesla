import styled from "@emotion/styled";
import { Box } from "@mui/material"

const HomeWrapper = styled(Box)(()=>({
    position: 'relative',
}));

const SectionWrapper = styled(Box)(()=>({
    
}));

export { HomeWrapper, SectionWrapper };